This module uses the Video module and integrates it with the Opigno module.
Creates Video content type, a view and sets default permissions,
if using the Opigno distribution.
Og module is required and the content type is set as an Organic Group content.
